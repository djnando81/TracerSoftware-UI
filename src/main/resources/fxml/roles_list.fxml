<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<BorderPane xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.tracersoftware.roles.controller.RolesListController">
    <center>
        <VBox spacing="8" style="-fx-padding:12;" fillWidth="true">
            <HBox spacing="8">
                <Label text="Roles" style="-fx-font-size:16px; -fx-font-weight:bold;" />
                <Region HBox.hgrow="ALWAYS" />
                <Button fx:id="btnNew" text="➕ Nuevo" styleClass="button-blue btn-primary icon-btn" />
            </HBox>
            <StackPane VBox.vgrow="ALWAYS">
                <TableView fx:id="tableRoles" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn text="ID" fx:id="colId" />
                        <TableColumn text="Nombre" fx:id="colNombre" />
                        <TableColumn text="Permisos" fx:id="colPermisos" />
                        <TableColumn text="Acciones" fx:id="colActions" />
                    </columns>
                </TableView>
            </StackPane>
            <com.tracersoftware.common.controls.PaginatorControl fx:id="paginator" />
        </VBox>
    </center>
    
</BorderPane>
